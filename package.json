{
    "name": "@barihari/treasury-foundations-test",
    "version": "0.0.0",
    "description": "Foundational Design Tokens for Treasury Platform, synced from Figma.",
    "main": "src/tokens/tailwind-theme.css",
    "scripts": {
      "build": "echo 'No build step required for simple CSS export' && exit 0"
    },
    "publishConfig": {
      "@barihari:registry": "https://npm.pkg.github.com/"
    }
  }
  
